For this backend python code to be valid, the following must be installed ("pip install" from terminal) into the virtual environment where you are running the python:

- flask
- pandas
- scikit-learn
- yfinance
- matplotlib
